var options = {
    strings: ['Software Engineer'],
    typeSpeed: 60,
    startDelay: 400,
    backDelay: 700,
    backSpeed: 20,
    smartBackspace: 1,
    loop: false,
  };
  
  var typed = new Typed('#subtitle', options);